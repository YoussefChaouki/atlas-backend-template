name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[dev]"

      - name: Linting
        run: |
          ruff check .
          ruff format --check .
          mypy src/

      - name: Create .env for Docker
        run: |
          cat <<EOF > .env
          PROJECT_NAME="Atlas Test"
          ENVIRONMENT="ci"
          POSTGRES_USER=atlas
          POSTGRES_PASSWORD=atlas_password
          POSTGRES_DB=atlas_db
          POSTGRES_HOST=db
          POSTGRES_PORT=5432
          REDIS_HOST=redis
          REDIS_PORT=6379
          EOF

      - name: Start Docker Stack
        run: docker compose up -d --build

      - name: Wait for Postgres
        run: |
          for i in {1..30}; do
            if docker compose exec -T db pg_isready -U atlas -d atlas_db; then
              echo "Postgres ready ✅"
              exit 0
            fi
            echo "Waiting for Postgres... ($i/30)"
            sleep 2
          done
          docker compose logs db
          exit 1

      - name: Run DB migrations (runner -> localhost)
        run: |
          # Le runner parle à Postgres via le port publié 5432
          POSTGRES_HOST=localhost alembic upgrade head

      - name: Verify tables exist
        run: |
          docker compose exec -T db psql -U atlas -d atlas_db -c "\dt"

      - name: Wait for API Health
        run: |
          for i in {1..30}; do
            if curl -fsS http://localhost:8000/health | grep -q "ok"; then
              echo "API is up ✅"
              exit 0
            fi
            echo "Waiting for API... ($i/30)"
            sleep 2
          done
          docker compose logs api
          exit 1

      - name: Run Tests
        run: pytest tests/ -v

      - name: Debug logs (only on failure)
        if: failure()
        run: |
          docker compose ps
          docker compose logs api --tail=200
          docker compose logs db --tail=200

      - name: Stop Docker Stack
        if: always()
        run: docker compose down -v
